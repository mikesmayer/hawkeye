
<%= link_to "Download CSV", { :controller => 'item_sales', :date_range => "#{@start_date}T#{@end_date}", :action => 'items', :format => :csv }, class: 'btn btn-primary btn-xs pull-right' %>

<table id="item-sales-table", data-source="<%= item_sales_path(format: :json) %>" class="table table-striped table-hover datatable">
  <thead>
    <tr>
		<th>Date</th>
		<th>Menu Item</th>
		<th>Category</th>
		<th>Quantity</th>
		<th>Net Price</th>
		<th>Meal4Meal</th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>