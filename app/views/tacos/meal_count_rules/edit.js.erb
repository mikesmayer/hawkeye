$('#tacos-rule-modal').html("<%= escape_javascript(render 'edit') %>");
$('#tacos-rule-modal').modal("show");

$('.datepicker').datepicker({
	format: 'yyyy/mm/dd',
	//endDate: '-1d',
	autoclose: false
});